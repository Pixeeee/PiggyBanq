{
  "name": "piggybanq",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}

